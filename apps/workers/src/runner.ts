import type { JobEnvelope, JobResult } from './contract.js';
export interface JobHandler<T=unknown>{(job:JobEnvelope<T>,signal:AbortSignal):Promise<void>}
export class WorkerRunner<T=unknown>{private stopping=false;private active=0;constructor(private readonly handler:JobHandler<T>,private readonly timeoutMs=30000,private readonly concurrency=10){}requestStop(){this.stopping=true}canAccept(){return !this.stopping&&this.active<this.concurrency}async run(job:JobEnvelope<T>):Promise<JobResult>{if(!this.canAccept())throw new Error('WORKER_NOT_ACCEPTING');this.active++;const controller=new AbortController();const timer=setTimeout(()=>controller.abort(new Error('job timeout')),this.timeoutMs);try{await this.handler(job,controller.signal);return{state:'COMPLETED',attempt:job.attempt}}catch(error){const attempt=job.attempt+1;return{state:attempt>=job.max_attempts?'DEAD_LETTERED':'RETRYING',attempt,errorCode:error instanceof Error?error.name:'UNKNOWN'}}finally{clearTimeout(timer);this.active--}}async gracefulShutdown(maxWaitMs=30000){this.stopping=true;const start=Date.now();while(this.active&&Date.now()-start<maxWaitMs)await new Promise(r=>setTimeout(r,50))}}
export function retryDelayMs(attempt:number,baseMs=500,capMs=60000){const exp=Math.min(capMs,baseMs*2**Math.max(0,attempt-1));return exp+Math.floor(Math.random()*Math.max(1,Math.floor(exp*.2)))}
