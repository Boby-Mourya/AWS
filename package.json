{
  "name": "pluggable-aws-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "services/*", "packages/*", "adapters/*"],
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "test:architecture": "node --test tests/architecture/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
